/**
 * The domain types live in packages/shared so the API validates and returns
 * exactly the shapes the screens use. Re-exported here so the web app keeps
 * importing from './domain/types.js'.
 */
export * from '@ai4rig/shared';
